For now, start the server with `npm run dev`. Server is running on port 3000, start server first, React after.

Run `apidoc -i src/ -o doc/` to regenerate the documentaion. It is regenerated in the `/doc` folder.
